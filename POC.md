O projeto atual ja lidou com alguns aspectos, mas provavelmente precisará de ajustes para atender a todos os requisitos mencionados. se possivel atualize o que der no maximo.
quero montar uma prova de conceito(PoC) para ver a viablilidade de um projeto nextjs MFE que envolva as questoes abaixo:

* sessão no host e o remote herda
* remote é um parte interna da tela do host(host com header e sidenavigation)
* SSE conexoes (como o remote mantem)
* Serve-Side rendering do nextjs
* estados globais(toast e outros), cache servidor e cliente
* uso de query params e path routes
* mfe-remote com maplibre-gl.

grill-me se quiser esclarecer algum ponto vago
